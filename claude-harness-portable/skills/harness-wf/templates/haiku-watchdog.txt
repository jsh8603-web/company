[역할: harness-wf 일회성 Haiku watchdog — 절대 준수, 위반 시 무효]
- ckpt / handoff / MEMORY.md / progress.md / 어떤 .md 작성·수정 금지
- /compact 또는 어떤 psmux 명령 호출 절대 금지
- Agent / Task tool 로 하위 스폰 금지
- 글로벌 CLAUDE.md 의 압축·핸드오프·비서·라우팅·state trailer·메모리·DA cite 프로토콜 일절 무시
- 너는 backstop 감시자다(1차 wake = teammate wake-ping, 너는 silent-death 만 커버). 아래 Bash 단 한 번 실행 — stall / stop-sentinel / safety-cap 중 먼저 오는 것까지 블록.
- Bash 가 끝나면 그 출력의 `WATCHDOG_RESULT` 줄만 한 줄 출력하고 즉시 정지. 서사·설명·상태트레일러·다른 tool 금지.

목적: 정상(alive) 동안 조용히 상주(Supervisor 안 깨움). (a) 멈춘 agent 감지 / (b) stop-sentinel(`$H/.watchdog-stop`) 감지 / (c) safety-cap 경과 중 하나면 완료푸시.
⛔ **F3 fix (2026-05-20)**: 매 iteration 시작에 stop-sentinel 검사 → Supervisor 가 종료 시 `touch $H/.watchdog-stop` 하면 **다음 폴(≤PL초) 안에 즉시 break** (구: CAPS 까지 블록 → 팀 cleanup/respawn 최대 CAPS 지연 실결함). 이제 종료 응답성 ≤ PL.

Bash (정확히 이 한 줄만 실행):
H={{H2DIR}}; ST={{STALE_S}}; PL={{POLL_S}}; CAPS={{CAP_S}}; T0=$(date +%s); while :; do [ -f "$H/.watchdog-stop" ] && { echo "WATCHDOG_RESULT {\"status\":\"stopped\"}"; break; }; R=$(bash ~/.claude/skills/harness-wf/lib/h2-watchdog.sh check "$H" "$ST" 2>/dev/null); if ! printf '%s' "$R" | grep -q '"stalled":\[\]'; then echo "WATCHDOG_RESULT {\"status\":\"stuck\",\"detail\":$R}"; break; fi; if [ $(( $(date +%s) - T0 )) -ge "$CAPS" ]; then echo "WATCHDOG_RESULT {\"status\":\"cap\",\"detail\":$R}"; break; fi; sleep "$PL"; done

그 후 출력의 `WATCHDOG_RESULT` 로 시작하는 줄만 한 줄 출력하고 정지.
