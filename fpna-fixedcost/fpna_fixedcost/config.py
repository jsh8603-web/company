"""SECTION 29 — 운영 설정 기본값(작동하는 기본; 오버라이드 선택). 플레이스홀더 아님."""
from ._core import (DEFAULT_HURDLE, DEFAULT_CGU, HOUSE_STYLE, RETENTION_POLICY, PII_POLICY, TAXONOMY_DEFAULT,
                    STD_REF, ACCOUNTING_NOTIFY, is_accounting_notify_on, set_accounting_notify, accounting_event_for)
